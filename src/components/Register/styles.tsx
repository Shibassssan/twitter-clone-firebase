import { css } from '@emotion/react';
import { Color } from '~/src/Style';

export const style = css`
  padding: 0 15px;
  display: grid;
  row-gap: 35px;
  margin-top: 15px;
  padding: 0 32px;

  & .emailRegister {
    padding: 0;
    display: grid;
    row-gap: 20px;
  }
`;
